export interface PropertyView {
  id: string;
  ipAddress?: string;
  userAgent?: string;
  propertyId: string;
  userId?: string;
  createdDate: string;
  updatedDate: string;
  deletedDate?: string;
}
